// var  x=5 ;
// var y=5;
// console.log("is both the numbers are same or not : " + (x==y) );
// console.log("is both the numbers are same or not " + x==y );
// console.log(`is both the numbers are same or not : ${x==y}`); //Using the template literal

// var num=15 ;    // num value can change 
// console.log((num++) + " " + (++num) );
// console.log(num)


//swap two number

// var a = 10 ;
// var b = 5 ;
// // Conditional Operator
// var result = a>b ? "Osm" : "good" ;
// console.log(result);
// var temp;
// temp=a;
// a=b ;
// b=temp;
// console.log(`The value of a and b is  : ${a} and ${b}`);

//without temp ;

// a = a + b ;
// b = a -b ;
// a = a - b ;
// console.log(`The value of a and b is  : ${a} and ${b}`);


/*
1. **Arithmetic Operators:**
   - Addition (`+`)
   - Subtraction (`-`)
   - Multiplication (`*`)
   - Division (`/`)
   - Modulus (Remainder) (`%`)
   - Increment (`++`)
   - Decrement (`--`)

2. **Assignment Operators:**
   - Assignment (`=`)
   - Addition Assignment (`+=`)
   - Subtraction Assignment (`-=`)
   - Multiplication Assignment (`*=`)
   - Division Assignment (`/=`)
   - Modulus Assignment (`%=`)

3. **Comparison Operators:**
   - Equal (`==`)
   - Not Equal (`!=`)
   - Strict Equal (`===`)
   - Strict Not Equal (`!==`)
   - Greater Than (`>`)
   - Less Than (`<`)
   - Greater Than or Equal (`>=`)
   - Less Than or Equal (`<=`)

4. **Logical Operators:**
   - Logical AND (`&&`)
   - Logical OR (`||`)
   - Logical NOT (`!`)

5. **Bitwise Operators:**
   - Bitwise AND (`&`)
   - Bitwise OR (`|`)
   - Bitwise XOR (`^`)
   - Bitwise NOT (`~`)
   - Left Shift (`<<`)
   - Right Shift (`>>`)
   - Zero-Fill Right Shift (`>>>`)

6. **Ternary (Conditional) Operator:**
   - Conditional (`? :`)

7. **String Operators:**
   - Concatenation (`+` for string concatenation)

8. **Type Operators:**
   - `typeof` (checks the data type of an expression)
   - `instanceof` (checks if an object is an instance of a particular class)

9. **Comma Operator:**
   - Comma (`,`) is used to separate expressions, and it evaluates them from left to right, returning the result of the last expression.

10. **Member Access Operators:**
    - Dot (`.`) and Bracket (`[]`) are used to access object properties and array elements.

11. **Function Operators:**
    - `function` is used to declare functions.

12. **Object Creation Operators:**
    - `new` is used to create instances of constructors.
*/